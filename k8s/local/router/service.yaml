apiVersion: v1
kind: Service
metadata:
  name: nginx-router-service
  namespace: app-oficina-tech
spec:
  type: NodePort
  selector:
    app: nginx-router
  ports:
    - port: 30080
      targetPort: 30080
      nodePort: 30080
      protocol: TCP
      name: http
